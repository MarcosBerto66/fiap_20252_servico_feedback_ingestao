spring.application.name=ingestao-feedback

# Configura��es padr�o para execu��o local
# Na AWS Lambda, as vari�veis de ambiente do template.yaml sobrescrevem estes valores.

DYNAMODB_TABLE_NAME=FeedbackTable
URGENCY_QUEUE_URL=https://sqs.sa-east-1.amazonaws.com/000000000000/urgency-feedback-queue

# Configura��o de Log para reduzir ru�do
logging.level.root=INFO
logging.level.org.springframework.cloud.function=INFO

# DEBUG LOCAL: fallback para garantir que a função correta seja usada em execução local
# Em ambientes reais (Lambda) prefira usar a variável de ambiente no template.yaml ou no ambiente
spring.cloud.function.definition=processarFeedback